# This rule is not used by the project rctf-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to rctf.osec.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: rctf-operator
    app.kubernetes.io/managed-by: kustomize
  name: challengeinstance-viewer-role
rules:
- apiGroups:
  - rctf.osec.io
  resources:
  - challengeinstances
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - rctf.osec.io
  resources:
  - challengeinstances/status
  verbs:
  - get
