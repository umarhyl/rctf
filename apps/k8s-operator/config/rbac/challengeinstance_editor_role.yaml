# This rule is not used by the project rctf-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the rctf.osec.io.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: rctf-operator
    app.kubernetes.io/managed-by: kustomize
  name: challengeinstance-editor-role
rules:
- apiGroups:
  - rctf.osec.io
  resources:
  - challengeinstances
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - rctf.osec.io
  resources:
  - challengeinstances/status
  verbs:
  - get
