import { response } from '../internal'

export const GoodAllChallengeSolvesDeleteV2 = response(
  'goodAllChallengeSolvesDeleteV2',
  {
    status: 200,
    message: 'All solves for this team were revoked.',
  }
)
